diff --git a/node_modules/react-native-reanimated/lib/module/createAnimatedComponent/NativeEventsManager.js b/node_modules/react-native-reanimated/lib/module/createAnimatedComponent/NativeEventsManager.js
index 2b0a2dd..ab482c4 100644
--- a/node_modules/react-native-reanimated/lib/module/createAnimatedComponent/NativeEventsManager.js
+++ b/node_modules/react-native-reanimated/lib/module/createAnimatedComponent/NativeEventsManager.js
@@ -58,6 +58,9 @@ export class NativeEventsManager {
   getEventViewTag(componentUpdate = false) {
     // Get the tag for registering events - since the event emitting view can be nested inside the main component
     const componentAnimatedRef = this.#managedComponent._componentRef;
+    if (!componentAnimatedRef) {
+      return -1;
+    }
     if (componentAnimatedRef.getScrollableNode) {
       /*
         In most cases, getScrollableNode() returns a view tag, and findNodeHandle is not required. 
diff --git a/node_modules/react-native-reanimated/lib/module/createAnimatedComponent/createAnimatedComponent.js b/node_modules/react-native-reanimated/lib/module/createAnimatedComponent/createAnimatedComponent.js
index 69682cb..1b41a3d 100644
--- a/node_modules/react-native-reanimated/lib/module/createAnimatedComponent/createAnimatedComponent.js
+++ b/node_modules/react-native-reanimated/lib/module/createAnimatedComponent/createAnimatedComponent.js
@@ -54,7 +54,14 @@ function onlyAnimatedStyles(styles) {
 
 let id = 0;
 export function createAnimatedComponent(Component, options) {
-  invariant(typeof Component !== 'function' || Component.prototype && Component.prototype.isReactComponent, `Looks like you're passing a function component \`${Component.name}\` to \`createAnimatedComponent\` function which supports only class components. Please wrap your function component with \`React.forwardRef()\` or use a class component instead.`);
+  // RN 0.76 compat: auto-wrap function components in forwardRef
+  if (typeof Component === 'function' && !(Component.prototype && Component.prototype.isReactComponent)) {
+    var Original = Component;
+    Component = React.forwardRef(function(props, ref) {
+      return React.createElement(Original, Object.assign({}, props, { ref: ref }));
+    });
+    Component.displayName = 'AutoForwardRef(' + (Original.displayName || Original.name || 'Unknown') + ')';
+  }
   class AnimatedComponent extends React.Component {
     _styles = null;
     _isFirstRender = true;
@@ -172,12 +179,7 @@ export function createAnimatedComponent(Component, options) {
       } else {
         const hostInstance = findHostInstance(this);
         if (!hostInstance) {
-          /* 
-            findHostInstance can return null for a component that doesn't render anything 
-            (render function returns null). Example: 
-            svg Stop: https://github.com/react-native-svg/react-native-svg/blob/develop/src/elements/Stop.tsx
-          */
-          throw new ReanimatedError('Cannot find host instance for this component. Maybe it renders nothing?');
+          return;
         }
         const viewInfo = getViewInfo(hostInstance);
         viewTag = viewInfo.viewTag;
diff --git a/node_modules/react-native-reanimated/src/createAnimatedComponent/NativeEventsManager.ts b/node_modules/react-native-reanimated/src/createAnimatedComponent/NativeEventsManager.ts
index e912ae2..8bf1886 100644
--- a/node_modules/react-native-reanimated/src/createAnimatedComponent/NativeEventsManager.ts
+++ b/node_modules/react-native-reanimated/src/createAnimatedComponent/NativeEventsManager.ts
@@ -86,6 +86,10 @@ export class NativeEventsManager implements INativeEventsManager {
       // Paper
       _nativeTag?: number;
     };
+    // RN 0.76 compat: ref may be null for auto-wrapped function components
+    if (!componentAnimatedRef) {
+      return -1;
+    }
     if (componentAnimatedRef.getScrollableNode) {
       /*
         In most cases, getScrollableNode() returns a view tag, and findNodeHandle is not required. 
diff --git a/node_modules/react-native-reanimated/src/createAnimatedComponent/createAnimatedComponent.tsx b/node_modules/react-native-reanimated/src/createAnimatedComponent/createAnimatedComponent.tsx
index e101e03..576a33b 100644
--- a/node_modules/react-native-reanimated/src/createAnimatedComponent/createAnimatedComponent.tsx
+++ b/node_modules/react-native-reanimated/src/createAnimatedComponent/createAnimatedComponent.tsx
@@ -111,11 +111,20 @@ export function createAnimatedComponent(
   Component: ComponentType<InitialComponentProps>,
   options?: Options<InitialComponentProps>
 ): any {
-  invariant(
-    typeof Component !== 'function' ||
-      (Component.prototype && Component.prototype.isReactComponent),
-    `Looks like you're passing a function component \`${Component.name}\` to \`createAnimatedComponent\` function which supports only class components. Please wrap your function component with \`React.forwardRef()\` or use a class component instead.`
-  );
+  // RN 0.76 compat: auto-wrap function components in forwardRef.
+  // Multiple libs (react-native-screens, uniwind, gesture-handler) pass
+  // function components. Without forwardRef, refs are null → getScrollableNode
+  // crashes and Reanimated can't find host instances.
+  if (
+    typeof Component === 'function' &&
+    !(Component.prototype && Component.prototype.isReactComponent)
+  ) {
+    const Original = Component;
+    Component = React.forwardRef((props: any, ref: any) =>
+      React.createElement(Original, { ...props, ref })
+    ) as any;
+    (Component as any).displayName = `AutoForwardRef(${Original.displayName || Original.name || 'Unknown'})`;
+  }
 
   class AnimatedComponent
     extends React.Component<AnimatedComponentProps<InitialComponentProps>>
@@ -295,14 +304,9 @@ export function createAnimatedComponent(
       } else {
         const hostInstance = findHostInstance(this);
         if (!hostInstance) {
-          /* 
-            findHostInstance can return null for a component that doesn't render anything 
-            (render function returns null). Example: 
-            svg Stop: https://github.com/react-native-svg/react-native-svg/blob/develop/src/elements/Stop.tsx
-          */
-          throw new ReanimatedError(
-            'Cannot find host instance for this component. Maybe it renders nothing?'
-          );
+          // RN 0.76 compat: auto-wrapped function components may not resolve
+          // host instances immediately. Skip animation setup for this mount.
+          return;
         }
 
         const viewInfo = getViewInfo(hostInstance);
